#include "Engine.h"
int main() { //Shortest, most complex .cpp file that starts the engine 
	Engine engine; //(Every engines has something that starts them, like a starter !).
	engine.start(); 
	return 0; 
}